# Open decisions — Product Owner tracker

Source of truth for the eight questions TRD 19.4 requires Product Owner
sign-off on ("QAROR TALABI: Bu sakkiz savol javobsiz qolsa, texnik jamoa
taxmin qiladi..." — if these eight questions go unanswered, the technical
team guesses, and guesses turn into expensive rework and security
conflicts later). This file exists because those eight questions were
previously only mentioned scattered across CLAUDE.md prose and the TRD
itself — nowhere was there one place to see, at a glance, which are
resolved, which are still open, and which already missed their stated
deadline. An AI coding agent must not resolve any of these unilaterally;
this tracker's job is to make sure none of them get silently lost.

Status is assessed here against **actual codebase progress** (which stage
each area of work has reached), not the calendar — per CLAUDE.md, this
project reads TRD stage gates as an ordering/critical-path signal, never
a calendar commitment (no real team, one AI agent + one Product Owner).
"Overdue" below means: the stage whose *start or end* was supposed to gate
this decision has, in terms of actual work done, already been passed.

| ID | Question | Stated deadline | Blocks | Status |
|----|----------|-----------------|--------|--------|
| OD-001 | DODA faqat Hikmatullo uchunmi yoki keyinchalik SaaS bo'ladimi? | S1 oxiri | Tenant provisioning, billing dizayni | **Resolved, va Product Owner tomonidan qayta tasdiqlandi** — hozircha shaxsiy foydalanish uchun, lekin keyinchalik boshqa foydalanuvchilarga taqdim eta olish (sotish) imkoniyati bilan. Bu aynan multi-tenant (SaaS-shaped) arxitekturaning o'zi — Customer→Workspace→Membership stage 0'dan beri shu tarzda qurilgan, qo'shimcha o'zgarish talab qilmaydi. Ko'p qismning haqiqiy tenant-tenant izolyatsiyasi (RLS, `test_tenant_isolation.py`) allaqachon shu ko'p-foydalanuvchili maqsad uchun qurilgan/testlangan. |
| OD-002 | Birinchi real konnektor: Google Calendar, Gmail, Telegram yoki boshqa? | S5 oxiri | S6, S7 | **Resolved va qurildi — Telegram.** Product Owner qarori (ADR-007 "Accepted", pastga qarang). `tool_name → minimal risk_level` server-side siyosat qatlami (`domain/action/tool_policy.py`) qurildi. Product Owner o'z bot tokenini environment variable orqali (hech qachon chat/prompt matniga yozmasdan) taqdim etdi — shundan keyin haqiqiy connector (`infrastructure/telegram_client.py` + `telegram_relay.py`) qurildi: outbox relay'ning Redis Stream'idagi `action.ready.v1` xabarlarini o'qib, `telegram.send_message` action'larini haqiqiy Bot API `sendMessage` chaqiruvi orqali RUNNING→SUCCEEDED/FAILED'gacha olib boradi. **Halol cheklov**: bu muhitning o'zida haqiqiy bot token/chat'ga kirish yo'q, shuning uchun faqat outbox→Stream→consumer→DB pipeline'i real Postgres+Redis'ga qarshi tasdiqlangan — Telegram'ning o'z HTTP javobi test double bilan almashtirilgan (5 unit + 5 integration test). Real Bot API'ga qarshi haqiqiy tasdiqlash hali qilinmagan (production muhitida, haqiqiy token bilan). |
| OD-003 | Qaysi ma'lumot sinflari cloud AI providerga mutlaqo yuborilmaydi? | S3 oxiri | Redaction qatlami, prompt dizayni | **Open, overdue, va endi HAQIQIY jonli xavf — avvalgi "hali real harm yo'q" bahosi endi to'g'ri emas.** AI provider tanlovining o'zi ALLAQACHON hal qilindi (Product Owner'ning aniq ko'rsatmasi bilan: uchta provayder — OpenAI standart, Google Gemini, Anthropic Claude — ADR-008/ADR-009), va `doda.application.conversation_service.stream_message` endi HAQIQIY ishlaydigan orkestratsiya — foydalanuvchi yozgan xabarning TO'LIQ matni (hech qanday redaction/filtrlashsiz) tanlangan provayderga yuboriladigan tayyor holatda. Bugungi kunda real tashqi ta'sir yo'q FAQAT shu muhitning tarmoq siyosati OpenAI/Gemini/Claude'ning barcha API domenlarini bloklagani uchun (ADR-008/ADR-009'dagi halol chegara) — real API kaliti ulangan ZAHOTI, OD-003 hal qilinmagan holda, foydalanuvchi chatga yozgan har qanday PII/maxfiy matn filtrlashsiz tashqi provayderga ketadi. Demak bu endi "Chat qurilishidan oldin hal qilinishi kerak" emas — **Chat ALLAQACHON qurilgan, shuning uchun bu birinchi real API kalit ulanishidan OLDIN hal qilinishi SHART**, aks holda OD-005'ning o'z residency/maxfiylik qarorlari ma'nosiz qoladi. `docs/risk-register.md`'ning RISK-003 qatoriga ham shu yangilanish yozildi. |
| OD-004 | O'zbek tilidagi ovoz MVP talabimi yoki keyingi bosqichmi? | S1 oxiri | Scope va jamoa rejasi | **Reversed by explicit Product Owner decision — ovoz MVP uchun KERAK.** Bu avvalgi "de facto resolved (out of scope)" baholashni bekor qiladi — TRD 2.3'ning o'z "Ovozli interfeys... OUT OF SCOPE" qatoriga zid, lekin bu aynan TRD 19.4'ning o'zi kutgan holat: yakuniy so'z har doim Product Owner'niki, hujjatning standart bandi emas. **Amalga oshirish hali BOSHLANMAGAN va yangi, hal qilinmagan savol ochadi**: qaysi STT/TTS provayder (RISK-010: "O'zbek tilida sifat pariteti" — ko'pchilik provayderlar o'zbek tilini yaxshi qo'llab-quvvatlamaydi, buni oldindan tekshirish kerak), qaysi UI oqimi (chat interfeysi hali qurilmagan — FR-CONV, ovoz odatda shu ustiga quriladi). Bu, connector tanlovi kabi, alohida arxitektura qarori talab qiladi — provayder tanlanmasdan qurish spekulyativ bo'lardi, shuning uchun ataylab hali boshlanmadi (QOIDA 2). |
| OD-005 | Production hosting mamlakati va data residency talabi? | S2 boshlanishidan oldin | Butun infra | **Resolved — Product Owner aynan shu qarorni Claude'ga topshirdi ("serverlarni sen o'zing tanlab, yozib qo'ygin").** Tanlov: TRD 13.3'ning o'z MVP tavsiyasi bo'yicha **hybrid** arxitektura (ma'lumotlar — Postgres/object storage — mahalliy/yaqin mintaqada, AI chaqiruvlari tashqi provayderga, lekin xom PII/maxfiy matn emas — OD-003 bilan bog'liq). Boshlang'ich provayder: **Hetzner Cloud (Germany)** — arzon, Docker Compose bilan mos keladigan VPS, hozirgi `docker-compose.yml` naqshiga to'g'ridan-to'g'ri mos. **Muhim chegara**: bu qaror hozircha faqat shaxsiy foydalanish (OD-001) uchun amal qiladi — agar loyiha kelajakda boshqa foydalanuvchilarga sotilsa (OD-001'ning "resellable later" qismi), O'zbekiston qonunchiligi bo'yicha haqiqiy data-residency talablarini yurist bilan alohida tekshirish kerak; bu yerdagi tanlov huquqiy maslahat emas, faqat texnik, qaytarib bo'ladigan boshlang'ich nuqta. Hali ECH QANDAY real infratuzilma joylashtirilmagan (faqat local dev + CI) — bu sof hujjatlashtirish, ADR-006 "Accepted" holatiga o'tkazildi. **Yangilanish**: Product Owner `natsecurity.uz`ning haqiqiy, o'ziga tegishli domen ekanini tasdiqladi va serverni aniq Claude'ning o'zi yaratishini so'radi — shu asosda `docker-compose.prod.yml`/`deploy/` yozildi (ADR-006'ning "Update" bo'limiga qarang), lekin real server/DNS hali yo'q (bu agent hisob/to'lov/DNS yarata olmaydi — `deploy/README.md`da aniq). **Ikkinchi yangilanish — amaliy qamrov toraytirildi**: Product Owner keyinroq aniq qildi — hozircha VPS ham, domen ham kerak emas, **Render.com'ning bepul reji joriy production sirti bo'lib qoladi**, boshqa ko'rsatma kelmaguncha. VPS/Hetzner/`natsecurity.uz` yo'nalishi bekor qilinmadi — kelajakda kerak bo'lganda ishlatish uchun kod/hujjat sifatida saqlanmoqda (`deploy/README.md`, `docker-compose.prod.yml`) — lekin bu HOZIRGI faol reja emas. Google login, workspace/task/action/kill-switch/audit/notification/eksport — barchasi Render'ning o'zida, real production'da tasdiqlangan (skrinshotlar orqali, `CLAUDE.md`ga qarang) — VPS faqat Redis/outbox-relay/telegram-relay (haqiqiy Telegram xabar yuborish) va maxsus domen qo'shadi, ikkalasi ham hozircha talab qilinmagan. |
| OD-006 | R5 actionlar v1'da bloklanadimi yoki dual approval bilan ruxsatmi? | S6 oxiri | Approval dizayni | **Open, not yet overdue** — stage S6 (Expansion) has not started. This is the same gap already documented in CLAUDE.md regarding the global (platform-wide) kill switch and Platform Owner's R5 dual-control requirement (TRD 2.2): no `platform_owner` role or two-approver mechanism exists yet, and per this row, the TRD itself has not decided whether R5 is even allowed in v1 at all — building dual-control infrastructure before this is answered would risk building the wrong thing. |
| OD-007 | Retention: chat, memory, audit va fayl uchun aniq muddatlar? | S4 oxiri | Retention job, huquqiy tekshiruv | **Open, not yet overdue** — stage S4 (Operations) has not started. NFR-DATA-002 ("Retention job hisoboti") has no implementation yet; nothing to retire early against an undefined retention period. |
| OD-008 | MVP uchun oylik infra va AI token byudjeti? | S2 oxiri | Model routing, limitlar | **Resolved — Product Owner aniq oraliq berdi: $20–$100/oy.** Bu yakuniy qat'iy raqam emas, boshlang'ich ishchi diapazon — NFR-COST-001 ("Customer/workspace/model bo'yicha byudjet va alert") hali qurilmagan, chunki hech qanday AI/model chaqiruv kodi yo'q (ADR-004 bilan bir xil bog'liqlik). Real AI integratsiyasi boshlanganda bu diapazon ichida aniq chegara (masalan ogohlantirish $20'da, qattiq to'xtatish $100'da) kod darajasida belgilanishi kerak. |

## What "overdue" actually means here

Three items (OD-003, OD-005, OD-008) have passed the stage gate the TRD
assigned them, purely because this project's actual build order didn't
wait for them — none of the three have caused a real defect yet, because
the work that depends on them (AI/prompt calls, real infrastructure
deployment, budget enforcement) hasn't started. They are listed as
overdue so they get closed *before* that dependent work starts, not
discovered as a blocker in the middle of it.

## Ownership

Per TRD 19.4, every one of these is explicitly a Product Owner decision —
not a technical/architecture call. This file is a tracker, not a request
to be actioned by whoever reads it next; update the Status column here
when Hikmatullo actually decides one, and update the corresponding ADR
(if one exists — ADR-006 for OD-005, ADR-007 for OD-002) from "Open" to
"Accepted" in the same change.
