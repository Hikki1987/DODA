# Risk register — TRD 19.1 tracker

Source of truth for the ten risks TRD 19.1 names, each with a stated
likelihood/impact, a mitigation, and an owner. This file exists for the
same reason `docs/open-decisions.md` does: before this, RISK-001..010 were
never referenced anywhere in the codebase or in CLAUDE.md by ID — a
requirement-traceability audit (comparing every FR-\*/NFR-\*/UC-\*/RISK-\*/
OD-\*/ASM-\* ID in the TRD against the actual codebase) found the risk
register was the one whole requirement family with **zero** IDs ever
cited, unlike every other family which had at least partial coverage.

Status below is assessed against **actual codebase state**, not the
calendar — same convention as `docs/open-decisions.md`. A risk whose
mitigation is a later-stage deliverable (AI/model calls, memory,
connectors) is marked **dormant**, not "overdue": it cannot yet have
materialized because the work it would threaten hasn't started. A risk
already partly addressed by something built is marked **partially
mitigated** with the concrete code/process it's backed by, not just an
intention.

| ID | Risk (TRD) | Ehtimollik / Ta'sir | TRD mitigatsiyasi | Egasi | Joriy holat |
|----|------------|----------------------|---------------------|-------|-------------|
| RISK-001 | Scope kengayishi | Yuqori / Yuqori | MVP exclusion ro'yxati va change control | Product Owner | **Qisman yengillashtirilgan, amalda** — TRD 2.3's exclusion list stays inside the TRD itself (no separate tracker), but the project's own QOIDA 2 ("so'ralmagan o'zgarish — change request, bug fix emas") and the repeated "Ataylab qurilmagan" / "so'ralmagan holda amalga oshirilmadi" notes throughout CLAUDE.md are change control actually being exercised on every session, not just a policy statement. |
| RISK-002 | Haddan tashqari avtonomiya | O'rta / Kritik | Risk tier, approval, dry-run | Tech Lead | **Largely mitigated** — risk-tier (R0–R5) + approval/step-up mechanics are fully built, tested, and concurrency-hardened (9.1/9.2). Dry-run (FR-ACT-002) is now built too: `domain/action/tool_policy.describe_action_preview` renders a human-readable preview ("Telegram orqali chat <id>ga xabar yuboradi: ...") on every `ActionOut` response (propose/get/list/consume), not just an echoed raw payload — registered tools get a real description, unregistered ones a labeled fallback that still shows the tool name and payload rather than hiding anything. |
| RISK-003 | Memory kontaminatsiyasi | O'rta / Yuqori | Provenance, ACL, o'chirish, tasdiqlash | AI Engineer | **Dormant** — the Knowledge/memory domain (stage 2) doesn't exist yet, so this risk cannot have materialized; no mitigation exists either, since there is nothing yet to contaminate. |
| RISK-004 | Vendor lock-in | O'rta / O'rta | Model gateway va portativ sxemalar | Tech Lead | **Mitigated by a real, proven implementation** — ADR-004's gateway is now built (ADR-008: OpenAI; ADR-009: Gemini + Claude, added via an explicit Product Owner scope expansion). The portability claim isn't speculative: adding the second/third provider required zero changes to `doda.ai.types`/`doda.ai.port`/the application or API layers, only new adapter modules — the one concrete test this risk's mitigation could be put to. Remaining gap, distinct from lock-in: no real end-to-end call to any of the three providers has been made (this environment's network egress policy blocks all three APIs — see ADR-008/ADR-009's own honest-limitation sections). OD-003 (which data classes may never reach a provider) is now **resolved for v1's actual data model** — `doda.ai.outbound_guard` blocks a chat message that looks like it contains a live credential before it is even persisted, and the two other sensitive classes considered (server-side secrets, single-use security tokens) were already structurally unreachable by the AI layer; see that row in `docs/open-decisions.md` for the explicit scope limit (pattern-matching, not general PII/DLP). |
| RISK-005 | Token/xarajat o'sishi | Yuqori / Yuqori | Byudjet, routing, caching, summarization | Product Owner | **Partially mitigated, now with real enforcement code** — OD-008 (monthly AI/infra budget, $20-$100) was resolved by Product Owner and `doda.application.ai_budget_service` enforces a real, concurrency-safe per-customer-month hard cap (`SELECT ... FOR UPDATE`-locked ledger, checked BEFORE every provider call) plus a separate DEEP-mode per-request cost ceiling. Not yet built: model routing by cost/complexity, caching, and summarization to reduce token spend — today's mitigation is a hard stop, not cost reduction. Pricing figures themselves are a multi-source web estimate, not a primary-source confirmation (`doda.infrastructure.ai_pricing`'s own docstring) — this environment cannot reach any provider's pricing page to verify directly. |
| RISK-006 | Xavfsizlik illyuziyasi (demo = production) | O'rta / Kritik | Live integratsiya va red-team gate'lari | QA/Security | **Best-mitigated risk in the project** — "DEMO ≠ PRODUCTION" is the codebase's first house rule (CLAUDE.md "Qat'iy qoidalar" #1): no module is CLOSED without real Postgres/Redis/HTTP/Playwright verification, and five `security-review` passes have now been run against the accumulated diff (the 4th against the entire PR diff at the time, zero findings; the 5th scoped to everything added since — Telegram, real Google OIDC, deployment infra, and the full multi-provider AI chat system — found one real Medium-severity issue). A dedicated red-team *gate* (an adversarial pass run by someone other than the builder, gating a release) doesn't formally exist — the security reviews so far are self-run by the same agent that wrote the code, which is a real difference from the TRD's "red-team gate'lari," even though the practical effect (finding and fixing real vulnerabilities before they ship) has repeatedly held up — though notably, the auditor bug below was NOT caught by any of the first four security-review passes; it surfaced from coverage measurement pointing at dead code, a reminder that a clean security-review result is not the same claim as "no vulnerabilities exist," only "this method found none." A sharper instance of this exact risk was later found and fixed: `CustomerRole.AUDITOR` was documented as read-only in three separate places (TRD 10.2, `domain/security/roles.py` twice) and enforced in none — an auditor added to a workspace held full write authority there. Nothing about that was a demo/production difference; it was a control that existed only in prose. The lesson recorded in CLAUDE.md is the generalisable one: a documented security property with no test is not a control, and coverage measurement is what surfaced it. **The 5th review's finding is a variant of the same lesson from a different angle**: a candidate the 3rd review had explicitly examined and dismissed (a plain member guessing another caller's freely-chosen `uuid.uuid4()` Idempotency-Key to replay-fetch a pending approval's one-time nonce) stopped being a false positive the moment new code (the chat tool-calling bridge) started deriving that same key deterministically from data — a `conversation_id`/`call_id` pair — that the workspace's own chat API legitimately discloses to every member via ordinary GET endpoints. Neither the key-derivation code nor the replay-lookup code was individually wrong; the vulnerability existed only in the combination, which is exactly why a dismissed finding from an earlier review must be re-examined whenever the assumption it rested on ("this value is unguessable") changes, rather than treated as permanently closed. Fixed in both the manual API path (`api/actions.py`) and the chat path (`ai_tools.py`) by withholding the replayed Approval unless the replaying caller's `actor_id` matches the Action's original proposer — proven with the same revert-test-restore discipline as every other fix in this codebase (two dedicated regression tests, each shown to fail against the pre-fix code before the fix was restored). |
| RISK-007 | Past ishonch — foydalanuvchi actionni topshirmaydi | O'rta / Yuqori | Preview, evidence, audit, undo | Product Owner | **Now largely mitigated — all four named mechanisms are built.** The audit trail (FR-AUD-001/002/004) was already thorough. Since then: "preview" (FR-ACT-002, see RISK-002's row) now renders a human-readable action description on every response; "evidence" (FR-AUD-005) is a real, exportable per-trace package (`GET /v1/customers/{id}/audit/evidence-package`) carrying every event's self-consistency check plus the full customer chain-verification result, not just the pre-existing personal-data export or chain-verify endpoint; "undo" (FR-CTL-005) is the FR-ACT-009 cancel button, scoped narrowly to Action's own reversible states (READY/RUNNING) and hidden for every terminal or awaiting-approval one — see CLAUDE.md for why this narrow reading matches the TRD's own section structure rather than a general "undo anything" feature. |
| RISK-008 | Kalit shaxsga bog'liqlik (bus factor) | Yuqori / Yuqori | ADR, hujjatlar, juftlikda ishlash | Tech Lead | **Partially addressed by design, one mitigation structurally unavailable** — ADRs and CLAUDE.md's exhaustive chronological log are real, working documentation practice. "Juftlikda ishlash" (pair programming) isn't a gap so much as inapplicable to this project's actual team shape (ASM-001: one AI coding agent + one Product Owner, no human engineering team) — already reinterpreted at the top of CLAUDE.md rather than silently ignored. |
| RISK-009 | Data residency qarorining kechikishi | Yuqori / Yuqori | OD-005 ni S2 gacha yopish | Product Owner | **The predicted delay happened, then was resolved — matching the TRD's own risk narrative exactly.** OD-005 did pass its S2 stage gate before being decided (the concrete materialization this risk named), then the Product Owner resolved it: a hybrid Hetzner/VPS architecture chosen as the eventual target (ADR-006), but the *actual* near-term surface narrowed further still — Render.com's free tier, explicitly not requiring the VPS/domain step at all for now. Real production traffic (Google login, workspace/task/action/audit/notification/export) already runs on Render, verified by the Product Owner's own screenshots. The specific legal exposure this risk names (real data-residency law for reselling to other users, not personal use) remains explicitly unresolved by design — `docs/open-decisions.md`'s OD-005 row states plainly that today's choice is "texnik, qaytarib bo'ladigan boshlang'ich nuqta", not legal advice — so it stays flagged here rather than closed. |
| RISK-010 | O'zbek tilida sifat pariteti past bo'lishi | O'rta / Yuqori | EVAL-LANG to'plami, model routing | AI Engineer | **Still dormant in substance, though the code that would surface it now exists** — `doda.application.conversation_service`/three real provider adapters (ADR-008/ADR-009) are built, and `backend/scripts/run_ai_eval_suite.py` (Uzbek-language task suite) exists to measure this — but this environment cannot reach any of the three providers' real APIs, so the suite has only ever been run against `NullModelGateway`'s fixed reply, never a real model's Uzbek output. Quality parity genuinely cannot be measured until a real key is configured and the suite is re-run against it. Separately, the same traceability audit that produced this file also found **NFR-I18N-001** (externalized uz/ru/en strings, no hardcoding) was never checked: today's frontend is 100% hardcoded Uzbek with no i18n library at all — a related but distinct gap (UI string externalization vs. AI-response language quality). **Investigated for real (not just re-flagged) and deliberately deferred, not built**: measuring the actual scope (11 page/component files, ~2700 lines, an estimated 180-220 distinct UI strings, several woven into dynamic template expressions rather than plain literals) showed this would be a large, mechanical, whole-frontend rewrite — real regression risk for a requirement whose acceptance criterion ("Lint qoidasi") can only be satisfied by migrating every existing string up front, not incrementally (a partial migration would leave the lint gate either failing on the untouched half or not actually enforcing anything, and this codebase's own house rule is no half-finished implementations). Weighed against that cost: OD-001's own resolution is "shaxsiy foydalanish uchun, kelajakda boshqalarga taqdim etish qobiliyati bilan" — DODA is being built for personal, Uzbek-language use today, with the underlying multi-tenant capability kept open for later, not with every future-facing feature (of which multi-language UI is squarely one) pre-built speculatively; no ru/en translation content exists or has been requested, and inventing a locale-switching mechanism with no real second-language content to switch to would be exactly the "texnik jamoa taxmin qiladi" pattern the TRD itself warns against for undecided scope. Deferred until either a real second-language need is stated, or the UI's own growth (Knowledge/RAG, more pages) makes doing this now cheaper than doing it later — retrofitting gets more expensive the longer it waits, so this is not a closed question, just not this session's to force. |

## Ownership

Per TRD 19.1, most of these risks are owned by roles (Product Owner, Tech
Lead, QA/Security, AI Engineer) this project doesn't have as separate
humans — they collapse onto Hikmatullo To'rayev (Product Owner) and
whichever AI coding agent session is active (standing in for Tech
Lead/QA/AI Engineer). This file is a tracker, not an instruction to act:
a "dormant" risk should stay dormant until the domain it concerns
actually starts being built, not be preemptively mitigated against
nothing. Update the "Joriy holat" column here when a mitigation is
actually built, and cross-reference the relevant ADR/OD/CLAUDE.md entry
in the same change, matching `docs/open-decisions.md`'s convention.
